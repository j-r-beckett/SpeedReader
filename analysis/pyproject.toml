[project]
name = "analysis"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = ["marimo", "pandas", "seaborn", "bench"]

[tool.uv.sources]
bench = { path = "bench", editable = true }

[tool.marimo.runtime]
auto_reload = "lazy"
auto_instantiate = false
